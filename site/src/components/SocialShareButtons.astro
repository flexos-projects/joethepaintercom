```astro
---
import Button from './ui/Button.astro';

interface Props {
  title: string;
  url: string;
  text?: string;
  class?: string;
}

const {
  title,
  url,
  text = `Check out this article: ${title}`,
  class: className,
} = Astro.props;

const encodedUrl = encodeURIComponent(url);
const encodedTitle = encodeURIComponent(title);
const encodedText = encodeURIComponent(text);

const shareButtons = [
  {
    name: 'Facebook',
    icon: 'M13.397 20.997v-8.192h2.768l.412-3.217h-3.18V7.206c0-.928.258-1.564 1.59-1.564h1.7V2.511a21.49 21.49 0 00-2.483-.127c-2.465 0-4.156 1.503-4.156 4.275v2.392h-2.77v3.217h2.77V21h3.397z',
    shareUrl: `https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}&quote=${encodedText}`,
    label: `Share on Facebook: ${title}`,
  },
  {
    name: 'Twitter',
    icon: 'M22.46 6c-.8.36-1.65.6-2.55.7-.7.75-1.7 1.25-2.8 1.25-2.05 0-3.72-1.67-3.72-3.72 0-.3.03-.6.08-.88C9.9 5.42 7.7 4.34 6.2 2.5a3.75 3.75 0 00-.5 1.85c0 1.29.65 2.45 1.64 3.12a3.75 3.75 0 01-1.65-.45v.05c0 1.8 1.28 3.29 2.97 3.63a3.75 3.75 0 01-1.68.06c.47 1.47 1.83 2.54 3.44 2.57C12.42 16.7 10.7 17.5 8.8 17.5c-.32 0-.64-.02-.95-.06 1.7 1.1 3.7 1.75 5.85 1.75 6.9 0 10.7-5.7 10.7-10.7v-.5c.7-.5 1.3-1.15 1.8-1.88z',
    shareUrl: `https://twitter.com/intent/tweet?url=${encodedUrl}&text=${encodedText}`,
    label: `Share on X (formerly Twitter): ${title}`,
  },
  {
    name: 'LinkedIn',
    icon: 'M16 8a6 6 0 016 6v7h-4v-7a2 2 0 00-2-2 2 2 0 00-2 2v7h-4v-7a6 6 0 016-6zM4 21H0V9h4v12zM2 6a2 2 0 100-4 2 2 0 000 4z',
    shareUrl: `https://www.linkedin.com/shareArticle?mini=true&url=${encodedUrl}&title=${encodedTitle}&summary=${encodedText}`,
    label: `Share on LinkedIn: ${title}`,
  },
  {
    name: 'Email',
    icon: 'M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884zM18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z',
    shareUrl: `mailto:?subject=${encodedTitle}&body=${encodedText}%0A${encodedUrl}`,
    label: `Share via Email: ${title}`,
  },
];
---

<div class:list={['flex flex-wrap gap-4', className]} role="group" aria-label="Share this content on social media">
  {shareButtons.map((button) => (
    <Button
      variant="ghost"
      size="md"
      href={button.shareUrl}
      target="_blank"
      rel="noopener noreferrer"
      aria-label={button.label}
      class="flex items-center space-x-2 text-neutral-700 hover:text-primary transition-colors"
    >
      <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24">
        <path d={button.icon}></path>
      </svg>
      <span class="sr-only">{button.name}</span>
    </Button>
  ))}
</div>
```
---