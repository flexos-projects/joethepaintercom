---
interface Props {
  for: string;
  required?: boolean;
}

const { for: htmlFor, required = false, ...rest } = Astro.props;
---

<label
  for={htmlFor}
  class="block text-sm font-medium text-text-dark mb-1"
  {...rest}
>
  <slot />
  {required && <span class="text-error ml-1">*</span>}
</label>